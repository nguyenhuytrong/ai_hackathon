{
  "name": "carebridge-navigator",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend test -- --run"
  },
  "workspaces": [
    "frontend"
  ]
}
